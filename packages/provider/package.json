{
  "name": "fure-provider",
  "version": "1.0.0-alpha.2",
  "description": "Fure base authentication provider",
  "main": "dist/index.js",
  "types": "index.ts",
  "license": "MIT",
  "author": {
    "name": "Horacio Rivero",
    "email": "horacioriverods@gmail.com"
  },
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json",
    "prepublishOnly": "yarn run build"
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "@types/node-fetch": "^2.5.7",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
